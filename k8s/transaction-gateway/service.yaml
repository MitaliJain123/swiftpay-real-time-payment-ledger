apiVersion: v1
kind: Service
metadata:
  name: transaction-gateway
  namespace: swiftpay
spec:
  type: NodePort
  selector:
    app: transaction-gateway
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31776
